name: mod3
version: 0.1.0
publish_to: none
description: A new Flutter project.
environment:
  sdk: ">=3.1.2 <4.0.0"

dependencies:
  get: 4.6.6
  flutter:
    sdk: flutter
  http: ^1.1.0
  dio: ^5.3.2

dev_dependencies:
  flutter_lints: ^2.0.0
  flutter_test:
    sdk: flutter

flutter:
  uses-material-design: true
